// 音あてチートシート(試聴用)
//
// 使い方: 聴きたい行の先頭の「// 」を 1 つだけ外して保存する。
//         有効な行は常に 1 つだけにすること(複数外すと最後の 1 つだけ鳴る)。
//         SOUNDS.md とセットで使うと「名前 ↔ 音」が分かります。

// ▼ まず全ドラムを順番に一周聴く(この行の // を外す。._punchcard でタイミングも見える)
// s("bd sd rim hh oh cp lt mt ht rd cr").slow(2)._punchcard()

// ▼ ドラムを 1 つだけ鳴らす(下から 1 行えらんで // を外す)
s("bd")        // キック(バスドラム)
// s("sd")        // スネア
// s("cp")        // クラップ(手拍子)
// s("rim")       // リムショット
// s("hh")        // クローズドハイハット
// s("oh")        // オープンハイハット
// s("lt")        // 低いタム
// s("mt")        // 中くらいのタム
// s("ht")        // 高いタム
// s("rd")        // ライドシンバル
// s("cr")        // クラッシュシンバル

// ▼ シンセの波形を聴く(音程をつけて鳴らす)
// note("c3 e3 g3 c4").s("sine")      // サイン: 柔らかい
// note("c3 e3 g3 c4").s("sawtooth")  // のこぎり: 明るく豊か(ベース/リード向き)
// note("c3 e3 g3 c4").s("square")    // 矩形: 中空っぽい
// note("c3 e3 g3 c4").s("triangle")  // 三角: やや明るい

// ▼ 同じ名前の別サンプルを試す(: のうしろの数字を変える)
// s("bd:0 bd:1 bd:2 bd:3 bd:4")._punchcard()
